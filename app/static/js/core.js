// Legacy frontend entrypoints are intentionally kept as no-op stubs.
// The runtime UI is now provided by the packaged LightRAG WebUI under /chat.
window.__outlineLightRagLegacyFrontend = { disabled: true };
